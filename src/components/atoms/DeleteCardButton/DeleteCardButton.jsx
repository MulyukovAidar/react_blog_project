import React from 'react';
import styles from './styles.module.css';

const DeleteCardButton = () => (
  <button type="button" className={styles["delete-card-button"]}>X</button>
);

export default DeleteCardButton;
