import React from 'react';
import './LoginButton.css';

class LoginButton extends React.Component{
    render(){
        return(
            <button className="navbar-login">Логин</button>
        );
    }
}
export default LoginButton;
