import React from 'react';
import styles from './styles.module.css';

const LoginButton = () => (
  <button type="button" className={styles['navbar-login']}>Логин</button>
);
export default LoginButton;
