import React from 'react';
import styles from './styles.module.css';

const Footer = () => (
  <div className={styles.footer}>
    <span className={styles.quote}>Life is nothing but a game</span>
  </div>
);
export default Footer;
